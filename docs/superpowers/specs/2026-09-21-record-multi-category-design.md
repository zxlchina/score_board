# 录入积分多选原因

日期：2026-09-21

## 目标

录入积分时，同类型原因可多选。提交后按选中项各生成一条记录。

## 规则

- 原因卡片点击切换选中；切换奖励/扣除清空已选。
- 选 1 条：可改本次分值，可写备注（与现在一致）。
- 选 2 条及以上：不能改分，每条用该分类默认分；备注可选，写入每一条。
- 一批插入放在同一事务中。

## 非目标

- 跨类型混选
- 多选时为每个原因单独改分
