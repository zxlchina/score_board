# 积分计分板 — 部署说明

与 [中国象棋](../xiangqi/部署.md) 同一台 **lichzhang.net** 服务器。

- **本地源码**：`MyProject/score_board/`
- **访问地址**：https://lichzhang.net/tools/score_board/
- **部署目录**：`/data1/release/tools/score_board/`
- **`/tools/`** 预留给你后续其它工具（静态目录或单独反代），本应用只占 **`/tools/score_board`**

## 架构

| 路径 | 说明 |
|------|------|
| `https://lichzhang.net/tools/` | 以后可放工具入口页 / 其它静态工具 |
| `https://lichzhang.net/tools/score_board/` | 本计分板（Node @13001） |

## 本机

```bash
cd MyProject/score_board
npm install
npm run dev
```

## 日常部署

```bash
npm run deploy
```

等价于 `NEXT_BASE_PATH=/tools/score_board` 构建 → rsync 到 `/data1/release/tools/score_board/` → 远程 `restart.sh`。

## 服务器 `.env`

路径：`/data1/release/tools/score_board/.env`

```env
NODE_ENV=production
PORT=13001
ADMIN_PASSWORD=强口令
SESSION_SECRET=至少32位随机
DATABASE_PATH=./data/score.db
NEXT_BASE_PATH=/tools/score_board
HOSTNAME=0.0.0.0
```

## Nginx（仅反代 score_board，勿占用整个 /tools/）

在 `lichzhang.net` 的 server 块中（**不要**用 `location ^~/tools` 包住全部 tools）：

```nginx
location ^~/tools/score_board {
    proxy_pass http://127.0.1.1:13001;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}
```

修改后：`nginx -t && systemctl reload nginx`  
**勿**在 `sites-enabled/` 里留 `.bak` 备份文件。

## 检查

```bash
curl -sI 'https://lichzhang.net/tools/score_board/'
ssh lichzhang.net 'curl -s -o /dev/null -w "%{http_code}\n" http://127.0.1.1:13001/tools/score_board/ -L'
```

## 备份

`/data1/release/tools/score_board/data/score.db`
